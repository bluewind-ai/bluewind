// Fetch-only script, no imports allowed but benefits from a dedicated highly efficient runtime

export async function main(smartlead_campaign_id: object) {
  return {
    smartlead_campaign_id
  }
}
